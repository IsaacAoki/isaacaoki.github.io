# Local leftover from the first design direction. Untracked because it's
# 2.4 MB, and kept because the artboards it was generated from are gone,
# so this is the only surviving copy. Delete it whenever you like:
#   rm -rf design
design/
